import React from 'react';

const NotMatch = () => {
    return (
        <div style={{color: 'white'}}>
            <h1>404 Not found the content.</h1>
        </div>
    );
};

export default NotMatch;